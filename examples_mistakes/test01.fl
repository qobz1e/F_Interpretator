(cond true 1 2)

(setq counter 0)
(func incrementcounter ()
  (setq counter (plus counter 1))
  counter)
(incrementcounter)
(incrementcounter)

(func makecounter ()
  (setq count 0)
  (lambda ()
    (setq count (plus count 1))
    count))
(setq c makecounter)
(c)
(c)